#include <stdio.h>

/**
 * main - prints all single digit numbers of base 10 starting from 0
 * Description: all code should be in the main function
 * Return: 0
 */

int main(void)
{
	int i;

	for (i = 0; i <= 9; i++)
	{
		printf("%i", i);
	}
	printf("\n");
	return (0);
}
