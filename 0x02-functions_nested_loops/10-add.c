#include "main.h"

/**
 * add - adds two integers
 * @numa: first integer to be added
 * @numb: second integer to be added
 * Return: results of adding the two integers
 */
int add(int numa, int numb)
{
	return (numa + numb);
}
